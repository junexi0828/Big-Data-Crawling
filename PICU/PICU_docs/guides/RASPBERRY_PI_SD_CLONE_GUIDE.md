# 라즈베리 파이 SD 카드 복제 및 클러스터 설정 가이드

**작성 일시**: 2025-11-30
**대상**: 라즈베리 파이 4대 클러스터 초기 설정
**버전**: 1.0

---

## 📋 목차

1. [SD 카드 복제 방법](#sd-카드-복제-방법)
   - [방법 0: 자동화 스크립트 사용 (가장 간편)](#방법-0-자동화-스크립트-사용-가장-간편-)
   - [방법 1: macOS에서 dd 명령어 사용](#방법-1-macos에서-dd-명령어-사용-수동-복제)
   - [방법 2: Raspberry Pi Imager 사용](#방법-2-raspberry-pi-imager-사용)
   - [방법 3: Balena Etcher 사용](#방법-3-balena-etcher-사용)
2. [복제 후 각 노드별 필수 수정 사항](#복제-후-각-노드별-필수-수정-사항)
3. [네트워크 설정](#네트워크-설정)
4. [검증 및 테스트](#검증-및-테스트)

---

## SD 카드 복제 방법

### 방법 0: 자동화 스크립트 사용 (가장 간편) ⭐

**`prepare_sd_cards.sh` 스크립트를 사용하면 각 노드별 설정을 자동으로 적용할 수 있습니다.**

#### 전체 워크플로우

```
1. Ubuntu 이미지를 SD 카드 4장에 굽기
   ↓
2. SD 카드를 하나씩 Mac에 연결
   ↓
3. prepare_sd_cards.sh 실행 → 노드 선택
   ↓
4. SD 카드 제거 → 라즈베리파이 삽입
```

#### 상세 단계

**STEP 1: Ubuntu 이미지를 SD 카드 4장에 굽기**

```bash
# Raspberry Pi Imager 또는 Balena Etcher 사용
# Ubuntu Server 20.04 LTS (64-bit) ARM 이미지
# 4장 모두 동일한 이미지로 설치
```

**STEP 2: 각 SD 카드별로 스크립트 실행**

```bash
# 프로젝트 디렉토리로 이동
cd /Users/juns/code/personal/notion/pknu_workspace/bigdata/PICU/deployment

# SD 카드 #1을 Mac에 연결
./prepare_sd_cards.sh
# 메뉴에서 1 선택 (Master Node)
# SD 카드 제거 → 라즈베리파이 #1에 삽입

# SD 카드 #2를 Mac에 연결
./prepare_sd_cards.sh
# 메뉴에서 2 선택 (Worker Node 1)
# SD 카드 제거 → 라즈베리파이 #2에 삽입

# SD 카드 #3를 Mac에 연결
./prepare_sd_cards.sh
# 메뉴에서 3 선택 (Worker Node 2)
# SD 카드 제거 → 라즈베리파이 #3에 삽입

# SD 카드 #4를 Mac에 연결
./prepare_sd_cards.sh
# 메뉴에서 4 선택 (Worker Node 3)
# SD 카드 제거 → 라즈베리파이 #4에 삽입
```

**스크립트 실행 화면 예시:**

```
╔════════════════════════════════════════════════════╗
║                                                    ║
║       CoinTicker Hadoop Cluster                    ║
║       SD 카드 준비 도구                            ║
║                                                    ║
╚════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
어느 노드용 SD 카드를 준비하시겠습니까?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  1) Master Node       (raspberry-master)   192.168.0.100
  2) Worker Node 1     (raspberry-worker1)  192.168.0.101
  3) Worker Node 2     (raspberry-worker2)  192.168.0.102
  4) Worker Node 3     (raspberry-worker3)  192.168.0.103

  5) 현재 설정 확인
  0) 종료

선택 (0-5): 1

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 Master Node 설정 중...
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ SD 카드 감지됨: /Volumes/bootfs
✓ raspberry-master로 설정 완료

📌 현재 설정된 hostname: raspberry-master

╔════════════════════════════════════════════════╗
║  ✓ Master Node SD 카드 준비 완료!             ║
╚════════════════════════════════════════════════╝

다음 단계:
  1. SD 카드를 안전하게 제거
  2. 라즈베리파이 #1 (Master)에 삽입
  3. 전원 켜기
```

**장점:**

- ✅ 각 노드별 설정 자동 적용
- ✅ 실수 방지 (메뉴 선택만 하면 됨)
- ✅ 현재 설정 확인 가능
- ✅ 반복 작업 간편

**주의사항:**

- SD 카드가 `/Volumes/bootfs` 또는 `/Volumes/system-boot`에 마운트되어 있어야 함
- Ubuntu 이미지를 구울 때 `worker-configs` 디렉토리와 설정 파일을 미리 준비해야 함
  - SD 카드의 boot 파티션에 `worker-configs/` 디렉토리 생성
  - 각 노드별 `user-data-worker1`, `user-data-worker2`, `user-data-worker3` 파일 준비

**스크립트가 필요한 파일 구조:**

```
/Volumes/bootfs/
├── user-data (현재 활성 설정)
└── worker-configs/
    ├── user-data-worker1
    ├── user-data-worker2
    └── user-data-worker3
```

**참고**: 스크립트를 사용하지 않고 수동으로 설정하려면 [방법 1](#방법-1-macos에서-dd-명령어-사용-수동-복제)을 참고하세요.

---

### 방법 1: macOS에서 dd 명령어 사용 (수동 복제)

```bash
# 1. SD 카드가 마운트되어 있는지 확인
diskutil list

# 2. SD 카드 언마운트 (예: /dev/disk2)
diskutil unmountDisk /dev/disk2

# 3. 이미지 생성 (원본 SD 카드에서)
sudo dd if=/dev/rdisk2 of=~/raspberry-pi-base.img bs=1m status=progress

# 4. 이미지를 다른 SD 카드에 복제 (3개)
# 각 SD 카드를 연결하고 diskutil list로 확인 후
sudo dd if=~/raspberry-pi-base.img of=/dev/rdisk3 bs=1m status=progress
sudo dd if=~/raspberry-pi-base.img of=/dev/rdisk4 bs=1m status=progress
sudo dd if=~/raspberry-pi-base.img of=/dev/rdisk5 bs=1m status=progress
```

### 방법 2: Raspberry Pi Imager 사용 (수동 복제)

1. Raspberry Pi Imager 다운로드 및 설치
2. "Use custom image" 선택
3. 원본 SD 카드에서 생성한 이미지 선택
4. 각 SD 카드에 순차적으로 복제
5. **복제 후 각 노드별 설정 필요** (호스트명, IP 등)

### 방법 3: Balena Etcher 사용 (수동 복제)

1. Balena Etcher 다운로드 및 설치
2. 원본 SD 카드에서 이미지 생성
3. 각 SD 카드에 순차적으로 복제
4. **복제 후 각 노드별 설정 필요** (호스트명, IP 등)

---

## ⚠️ 중요: 복제 후 필수 수정 사항

**SD 카드를 복제한 후에는 각 라즈베리 파이마다 반드시 다음 설정을 수정해야 합니다.**

### SSH 키에 대한 질문과 답변

**Q: 동일한 SSH 키로 4개 한번에 동작하거나 동일한 키로 각각 접속하는 거 안 되나요?**

**A: 네, 가능합니다!** 하지만 SSH 키는 두 종류가 있습니다:

1. **SSH 호스트 키 (서버 측)**:

   - 복제된 SD 카드는 동일한 호스트 키를 가집니다
   - **개발 환경에서는 그대로 사용해도 됩니다** ✅
   - 프로덕션에서는 각 노드마다 다른 키를 권장

2. **SSH 클라이언트 키 (개발 PC → 라즈베리 파이)**:
   - **동일한 클라이언트 키를 모든 라즈베리 파이에 복사하는 것이 권장됩니다** ✅
   - 하나의 키로 모든 노드에 접속 가능
   - 자동화 스크립트 작성이 편리

**결론**: 개발 환경에서는 호스트 키는 그대로 두고, 클라이언트 키만 설정하면 됩니다!

### 각 노드별 설정 요약

| 노드   | 호스트명            | IP 주소         | 역할                               |
| ------ | ------------------- | --------------- | ---------------------------------- |
| Node 1 | `raspberry-master`  | `192.168.0.100` | Master (NameNode, ResourceManager) |
| Node 2 | `raspberry-worker1` | `192.168.0.101` | Worker (DataNode)                  |
| Node 3 | `raspberry-worker2` | `192.168.0.102` | Worker (DataNode)                  |
| Node 4 | `raspberry-worker3` | `192.168.0.103` | Worker (DataNode)                  |

**참고**: IP 주소는 네트워크 환경에 맞게 조정하세요 (예: `192.168.0.x` 또는 `192.168.1.x`)

---

## 복제 후 각 노드별 필수 수정 사항

### 1. 호스트명 변경

각 라즈베리 파이에 SD 카드를 삽입하고 부팅한 후:

```bash
# Node 1 (Master)
sudo hostnamectl set-hostname raspberry-master

# Node 2 (Worker 1)
sudo hostnamectl set-hostname raspberry-worker1

# Node 3 (Worker 2)
sudo hostnamectl set-hostname raspberry-worker2

# Node 4 (Worker 3)
sudo hostnamectl set-hostname raspberry-worker3
```

또는 직접 파일 수정:

```bash
# /etc/hostname 파일 수정
sudo nano /etc/hostname
# 해당 노드의 호스트명으로 변경
```

### 2. 네트워크 설정 (고정 IP)

#### Ubuntu Server (netplan 사용)

각 노드에서 `/etc/netplan/` 디렉토리의 설정 파일을 수정:

**Node 1 (Master) - `/etc/netplan/50-cloud-init.yaml` 또는 유사한 파일:**

```yaml
network:
  version: 2
  ethernets:
    eth0: # 또는 인터페이스 이름에 맞게 수정
      dhcp4: no
      addresses:
        - 192.168.1.100/24
      gateway4: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
```

**Node 2 (Worker 1):**

```yaml
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: no
      addresses:
        - 192.168.1.101/24
      gateway4: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
```

**Node 3 (Worker 2):**

```yaml
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: no
      addresses:
        - 192.168.1.102/24
      gateway4: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
```

**Node 4 (Worker 3):**

```yaml
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: no
      addresses:
        - 192.168.1.103/24
      gateway4: 192.168.1.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
```

설정 적용:

```bash
sudo netplan apply
```

#### Raspberry Pi OS (dhcpcd 사용)

각 노드에서 `/etc/dhcpcd.conf` 파일 수정:

**Node 1 (Master):**

```bash
sudo nano /etc/dhcpcd.conf

# 파일 끝에 추가:
interface eth0
static ip_address=192.168.1.100/24
static routers=192.168.1.1
static domain_name_servers=8.8.8.8 8.8.4.4
```

**Node 2-4 (Workers):**

동일한 방식으로 각각 101, 102, 103으로 설정

적용:

```bash
sudo systemctl restart dhcpcd
```

### 3. /etc/hosts 파일 수정

**모든 노드에서 동일하게 수정:**

```bash
sudo nano /etc/hosts
```

다음 내용 추가 (네트워크에 맞게 IP 조정):

```
192.168.0.100 raspberry-master
192.168.0.101 raspberry-worker1
192.168.0.102 raspberry-worker2
192.168.0.103 raspberry-worker3
```

**참고**: IP 주소는 실제 네트워크 환경에 맞게 조정하세요 (예: `192.168.0.x` 또는 `192.168.1.x`)

### 4. SSH 키 설정

SSH 키에는 **두 가지 종류**가 있습니다:

#### 4-1. SSH 호스트 키 (서버 측) - 선택적

**복제된 SD 카드는 동일한 SSH 호스트 키를 가집니다.**

- **개발 환경**: 동일한 키를 그대로 사용해도 **문제없습니다** ✅
- **프로덕션 환경**: 보안을 위해 각 노드마다 다른 키를 사용하는 것이 권장됩니다

**동일한 키를 그대로 사용하는 경우:**

- 아무 작업도 필요 없습니다
- 개발 환경에서는 편리합니다

**각 노드마다 다른 키를 생성하려면 (선택적):**

```bash
# 각 노드에서 실행
sudo rm /etc/ssh/ssh_host_*

# 새로운 SSH 키 생성
sudo dpkg-reconfigure openssh-server

# 또는 수동 생성
sudo ssh-keygen -t rsa -f /etc/ssh/ssh_host_rsa_key -N ""
sudo ssh-keygen -t dsa -f /etc/ssh/ssh_host_dsa_key -N ""
sudo ssh-keygen -t ecdsa -f /etc/ssh/ssh_host_ecdsa_key -N ""
sudo ssh-keygen -t ed25519 -f /etc/ssh/ssh_host_ed25519_key -N ""

# SSH 서비스 재시작
sudo systemctl restart ssh
```

#### 4-2. SSH 클라이언트 키 (개발 PC → 라즈베리 파이 접속용) - 권장

**개발 PC에서 각 라즈베리 파이로 접속할 때 사용하는 키입니다.**

**동일한 SSH 클라이언트 키를 모든 라즈베리 파이에 복사하는 것이 권장됩니다** ✅

이렇게 하면:

- 하나의 키로 모든 노드에 접속 가능
- 패스워드 입력 없이 접속 가능
- 자동화 스크립트 작성이 편리

**설정 방법:**

```bash
# 1. 개발 PC에서 SSH 키 생성 (이미 있으면 생략)
ssh-keygen -t ed25519 -C "your_email@example.com"
# 또는
ssh-keygen -t rsa -b 4096

# 2. 각 라즈베리 파이에 동일한 공개 키 복사
ssh-copy-id ubuntu@raspberry-master
ssh-copy-id ubuntu@raspberry-worker1
ssh-copy-id ubuntu@raspberry-worker2
ssh-copy-id ubuntu@raspberry-worker3

# 또는 한 번에 복사 (스크립트)
for host in raspberry-master raspberry-worker1 raspberry-worker2 raspberry-worker3; do
    ssh-copy-id ubuntu@$host
done
```

**결과:**

- 개발 PC의 `~/.ssh/id_ed25519` (또는 `id_rsa`) 키 하나로
- 모든 라즈베리 파이에 패스워드 없이 접속 가능

**접속 테스트:**

```bash
# 패스워드 없이 접속되는지 확인
ssh ubuntu@raspberry-master
ssh ubuntu@raspberry-worker1
ssh ubuntu@raspberry-worker2
ssh ubuntu@raspberry-worker3
```

### 5. Machine ID 재생성 (선택적, 권장)

시스템 고유 ID를 재생성하여 각 노드를 구분:

```bash
# 각 노드에서 실행
sudo rm /etc/machine-id
sudo systemd-machine-id-setup
```

### 6. 재부팅 및 검증

각 노드를 재부팅:

```bash
sudo reboot
```

재부팅 후 각 노드에서 확인:

```bash
# 호스트명 확인
hostname

# IP 주소 확인
ip addr show eth0
# 또는
hostname -I

# 네트워크 연결 확인
ping -c 3 raspberry-master  # 다른 노드에서 실행
```

---

## 네트워크 설정

### 스위치/라우터 설정 확인

- 모든 라즈베리 파이가 같은 네트워크에 연결되어 있는지 확인
- 라우터에서 고정 IP 대역이 충돌하지 않는지 확인 (예: 192.168.0.100-103 또는 192.168.1.100-103)
- 실제 네트워크 환경에 맞게 IP 주소를 조정하세요

### 방화벽 설정 (필요시)

```bash
# Ubuntu UFW 사용 시
sudo ufw allow 22/tcp    # SSH
sudo ufw allow 9000/tcp  # HDFS NameNode
sudo ufw allow 9864/tcp  # HDFS DataNode
sudo ufw allow 8032/tcp  # YARN ResourceManager
sudo ufw allow 8042/tcp  # YARN NodeManager
```

---

## 검증 및 테스트

### 1. 네트워크 연결 테스트

**Master 노드에서:**

```bash
# 각 Worker 노드에 ping 테스트
ping -c 3 raspberry-worker1
ping -c 3 raspberry-worker2
ping -c 3 raspberry-worker3
```

**각 Worker 노드에서:**

```bash
# Master 노드에 ping 테스트
ping -c 3 raspberry-master
```

### 2. SSH 연결 테스트

**개발 PC에서:**

```bash
# 각 노드에 SSH 접속 테스트
ssh ubuntu@raspberry-master
ssh ubuntu@raspberry-worker1
ssh ubuntu@raspberry-worker2
ssh ubuntu@raspberry-worker3
```

### 3. 호스트명 확인

**각 노드에서:**

```bash
hostname
hostname -f
```

예상 결과:

- Node 1: `raspberry-master`
- Node 2: `raspberry-worker1`
- Node 3: `raspberry-worker2`
- Node 4: `raspberry-worker3`

---

## 자동화 스크립트 (선택적)

각 노드에서 실행할 수 있는 설정 스크립트 예시:

```bash
#!/bin/bash
# setup_node.sh - 각 노드에서 실행

NODE_NUMBER=$1  # 0=master, 1-3=workers
NODE_IP="192.168.0.10${NODE_NUMBER}"  # 네트워크에 맞게 조정

if [ $NODE_NUMBER -eq 0 ]; then
    HOSTNAME="raspberry-master"
else
    HOSTNAME="raspberry-worker${NODE_NUMBER}"
fi

# 호스트명 설정
sudo hostnamectl set-hostname $HOSTNAME

# /etc/hosts 수정 (네트워크에 맞게 IP 조정)
echo "192.168.0.100 raspberry-master" | sudo tee -a /etc/hosts
echo "192.168.0.101 raspberry-worker1" | sudo tee -a /etc/hosts
echo "192.168.0.102 raspberry-worker2" | sudo tee -a /etc/hosts
echo "192.168.0.103 raspberry-worker3" | sudo tee -a /etc/hosts

# SSH 키 재생성
sudo rm /etc/ssh/ssh_host_*
sudo dpkg-reconfigure openssh-server -f noninteractive

# Machine ID 재생성
sudo rm /etc/machine-id
sudo systemd-machine-id-setup

echo "✅ Node 설정 완료: $HOSTNAME ($NODE_IP)"
echo "재부팅이 필요합니다: sudo reboot"
```

사용법:

```bash
# Master 노드에서
chmod +x setup_node.sh
./setup_node.sh 0

# Worker 1에서
./setup_node.sh 1

# Worker 2에서
./setup_node.sh 2

# Worker 3에서
./setup_node.sh 3
```

---

## 주의사항

### 자동화 스크립트 사용 시

1. **SD 카드가 Mac에 마운트되어 있어야 함**: `/Volumes/bootfs` 또는 `/Volumes/system-boot`
2. **Ubuntu 이미지에 worker-configs 디렉토리 필요**: 스크립트가 설정 파일을 찾을 수 있어야 함
3. **각 SD 카드를 하나씩 처리**: 한 번에 하나의 SD 카드만 Mac에 연결

### 수동 복제 방법 사용 시

1. **SD 카드 복제는 시간이 오래 걸릴 수 있습니다** (32GB 기준 약 30분-1시간)
2. **복제 후 반드시 각 노드의 호스트명과 IP를 변경해야 합니다**
3. **SSH 키를 재생성하지 않으면 보안 문제가 발생할 수 있습니다**
4. **네트워크 설정을 잘못하면 접속이 불가능할 수 있으니 주의하세요**
5. **설정 변경 후 반드시 재부팅하세요**

---

## 다음 단계

설정이 완료되면 다음 문서를 참고하여 클러스터를 구성하세요:

- [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md) - 전체 배포 가이드
- [hadoop_project/docs/SETUP_GUIDE.md](../../hadoop_project/docs/SETUP_GUIDE.md) - Hadoop 클러스터 설정

---

## 🎯 빠른 시작 가이드 (자동화 스크립트 사용)

### 전체 워크플로우 요약

```
┌─────────────────────────────────────────────────────┐
│ 1. Ubuntu 이미지를 SD 카드 4장에 굽기              │
│    (Raspberry Pi Imager 사용)                       │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 2. SD 카드 #1 → Mac 연결                           │
│    ./prepare_sd_cards.sh → 1 선택 (Master)         │
│    → SD 카드 제거 → 라즈베리파이 #1 삽입           │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 3. SD 카드 #2 → Mac 연결                           │
│    ./prepare_sd_cards.sh → 2 선택 (Worker 1)       │
│    → SD 카드 제거 → 라즈베리파이 #2 삽입           │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 4. SD 카드 #3 → Mac 연결                           │
│    ./prepare_sd_cards.sh → 3 선택 (Worker 2)       │
│    → SD 카드 제거 → 라즈베리파이 #3 삽입           │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 5. SD 카드 #4 → Mac 연결                           │
│    ./prepare_sd_cards.sh → 4 선택 (Worker 3)       │
│    → SD 카드 제거 → 라즈베리파이 #4 삽입           │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 6. 모든 라즈베리파이 전원 켜기 (10-15분 대기)      │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 7. SSH 연결 확인                                    │
│    ssh ubuntu@192.168.0.100 (Master)                │
│    ssh ubuntu@192.168.0.101 (Worker 1)              │
│    ssh ubuntu@192.168.0.102 (Worker 2)              │
│    ssh ubuntu@192.168.0.103 (Worker 3)              │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 8. SSH 키 복사 (패스워드 없이 접속)                │
│    ssh-copy-id ubuntu@192.168.0.100                 │
│    ssh-copy-id ubuntu@192.168.0.101                 │
│    ssh-copy-id ubuntu@192.168.0.102                 │
│    ssh-copy-id ubuntu@192.168.0.103                 │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│ 9. 프로젝트 배포                                    │
│    ./deployment/setup_all_nodes.sh                  │
└─────────────────────────────────────────────────────┘
```

### 핵심 포인트

- ✅ **각 SD 카드의 유일한 차이는 hostname 한 줄뿐!**
- ✅ 스크립트가 자동으로 각 노드별 설정 적용
- ✅ 실수 방지 (메뉴 선택만 하면 됨)
- ✅ 복제 후 수동 설정 불필요 (스크립트 사용 시)

---

## 문제 해결

### 네트워크 연결이 안 될 때

1. 이더넷 케이블 연결 확인
2. 라우터/스위치 전원 확인
3. `ip addr show`로 IP 할당 확인
4. `sudo netplan apply` 또는 `sudo systemctl restart dhcpcd` 재실행

### SSH 접속이 안 될 때

1. SSH 서비스 상태 확인: `sudo systemctl status ssh`
2. 방화벽 설정 확인
3. 네트워크 연결 확인

### 호스트명이 변경되지 않을 때

1. `/etc/hostname` 파일 직접 수정
2. `/etc/hosts` 파일에 호스트명 추가
3. 재부팅

### prepare_sd_cards.sh 스크립트 오류

**문제**: "worker-configs 디렉토리를 찾을 수 없습니다"

**해결**:

1. SD 카드의 boot 파티션에 `worker-configs` 디렉토리 생성
2. 각 노드별 설정 파일 준비:
   ```bash
   # SD 카드 마운트 후
   mkdir -p /Volumes/bootfs/worker-configs
   # 각 노드별 user-data 파일 복사
   cp user-data-worker1 /Volumes/bootfs/worker-configs/
   cp user-data-worker2 /Volumes/bootfs/worker-configs/
   cp user-data-worker3 /Volumes/bootfs/worker-configs/
   ```

**문제**: "SD 카드가 마운트되지 않았습니다"

**해결**:

1. SD 카드가 Mac에 연결되어 있는지 확인
2. Finder에서 SD 카드가 보이는지 확인
3. 수동으로 마운트:
   ```bash
   diskutil mountDisk /dev/diskX
   ```

---

**작성자**: PICU 프로젝트 팀
**최종 수정**: 2025-01-XX
