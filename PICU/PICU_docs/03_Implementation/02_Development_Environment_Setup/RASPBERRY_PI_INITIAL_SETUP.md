# 라즈베리 파이 초기 설정 가이드

**작성 일시**: 2025-11-30
**대상**: 라즈베리 파이 초기 설정 및 SSH 키 설정
**버전**: 1.0

---

## 📋 목차

1. [SSH 인증 메커니즘 이해](#ssh-인증-메커니즘-이해)
2. [네트워크 구성 이해](#네트워크-구성-이해)
3. [초기 사용자 설정](#초기-사용자-설정)
4. [SSH 키 복사 (패스워드 없이 접속)](#ssh-키-복사-패스워드-없이-접속)
5. [접속 테스트](#접속-테스트)
6. [네트워크 설정 (유선/무선)](#네트워크-설정-유선무선)
7. [외부 접속 설정 (포트포워딩)](#외부-접속-설정-포트포워딩)

---

## SSH 인증 메커니즘 이해

### 🔑 SSH 공개키 인증 원리

SSH는 **공개키 암호화 방식**을 사용하여 안전하게 접속합니다.

#### 키 쌍의 구조

```
┌─────────────────────────────────────────────────────────┐
│  개발 PC (Mac)                                           │
├─────────────────────────────────────────────────────────┤
│  ~/.ssh/id_ed25519        🔒 개인키 (Private Key)        │
│  - 절대 공유하면 안 됨                                     │
│  - 서명(Signature)을 만드는 데 사용                        │
│                                                         │
│  ~/.ssh/id_ed25519.pub    🔓 공개키 (Public Key)         │
│  - 누구에게나 공유 가능                                    │
│  - 서명을 검증하는 데 사용                                 │
└──────────────────────┬──────────────────────────────────┘
                       │ (공개키만 복사)
                       ↓
┌─────────────────────────────────────────────────────────┐
│  라즈베리파이 (192.168.0.100)                            │
├─────────────────────────────────────────────────────────┤
│  /home/ubuntu/.ssh/authorized_keys                      │
│  - 허용할 공개키 목록                                      │
│  - 이 키를 가진 사람만 접속 허용                            │
└─────────────────────────────────────────────────────────┘
```

#### 인증 과정 (Challenge-Response)

```
1️⃣ 접속 시도
   Mac → 라즈베리파이: "192.168.0.100에 접속하고 싶습니다!"

2️⃣ 챌린지 발급
   라즈베리파이: "authorized_keys에서 공개키를 찾았습니다!"
   라즈베리파이: "이 랜덤 문자열(X7a9b2c...)을 개인키로 서명해보세요!"

3️⃣ 서명 생성
   Mac: 개인키(~/.ssh/id_ed25519)로 챌린지 서명
   Mac → 라즈베리파이: "서명 결과입니다!"

4️⃣ 서명 검증
   라즈베리파이: 공개키로 서명 검증
   ✅ 검증 성공 → 접속 허용
   ❌ 검증 실패 → "Permission denied"
```

#### 왜 비밀번호 없이 접속되는가?

- **공개키가 이미 등록**되어 있으면 비밀번호 불필요
- **개인키 소유 자체**가 신원 증명
- **user-data에 공개키 포함** → SD 카드 부팅 시 자동 등록

---

## 네트워크 구성 이해

### 🌐 로컬 네트워크 vs 외부 네트워크

#### 1. 같은 로컬 네트워크 (유선/무선 구분 없음)

```
┌─────────────────────────────────────────────────────────┐
│         같은 공유기 (192.168.0.1)                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Mac (무선)              라즈베리파이 Master (유선)        │
│  192.168.0.55      ←→    192.168.0.100                 │
│                                                         │
│  iPhone (무선)           라즈베리파이 Worker1 (유선)      │
│  192.168.0.20      ←→    192.168.0.101                 │
│                                                         │
│  노트북 (무선)           라즈베리파이 Worker2 (유선)       │
│  192.168.0.30      ←→    192.168.0.102                 │
│                                                         │
│  ✅ 모두 직접 SSH 접속 가능!                              │
│  ssh ubuntu@192.168.0.100                               │
└─────────────────────────────────────────────────────────┘
```

**핵심**:
- 같은 공유기에 연결되면 **유선/무선 상관없이** 같은 네트워크
- `192.168.0.x` 대역이 같으면 직접 통신 가능
- 포트포워딩 불필요

#### 2. 외부 네트워크에서 접속

```
┌─────────────────────────────────────────────────────────┐
│              인터넷 (공인 IP: 123.45.67.89)              │
└──────────────────────┬──────────────────────────────────┘
                       │ ❌ 사설 IP 직접 접근 불가
                       ↓
         ┌─────────────────────────────┐
         │   집 공유기                  │
         │   공인 IP: 123.45.67.89     │
         │   포트포워딩 설정 필요        │
         └─────────────────────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                             │
   Mac (무선)              라즈베리파이 (유선)
   192.168.0.55           192.168.0.100
```

**외부 접속 시**:
```bash
# ❌ 불가능 (사설 IP는 외부에서 접근 불가)
ssh ubuntu@192.168.0.100

# ✅ 포트포워딩 설정 후 가능
ssh -p 2222 ubuntu@123.45.67.89
```

---

## 초기 사용자 설정

### Ubuntu Server 설치 시

Ubuntu Server를 설치할 때 사용자 이름과 비밀번호를 설정합니다.

**권장 설정:**
- **사용자 이름**: `juns` (또는 원하는 이름)
- **비밀번호**: 안전한 비밀번호 설정 (예: `raspberry123!` 또는 더 강력한 비밀번호)

### 설치 후 사용자 추가 (선택적)

이미 설치되어 있다면 새 사용자를 추가할 수 있습니다:

```bash
# 라즈베리 파이에서 실행
sudo adduser juns
# 비밀번호 입력 및 정보 입력

# sudo 권한 부여
sudo usermod -aG sudo juns
```

---

## SSH 키 복사 (패스워드 없이 접속)

### 1단계: 개발 PC에서 SSH 키 확인

개발 PC(macOS)에서 이미 SSH 키가 있는지 확인:

```bash
# SSH 키 확인
ls -la ~/.ssh/

# 기존 키가 있다면 (id_rsa 또는 id_ed25519)
# 없다면 새로 생성
ssh-keygen -t ed25519 -C "your_email@example.com"
# 또는
ssh-keygen -t rsa -b 4096
```

### 2단계: 라즈베리 파이 IP 확인

라즈베리 파이에 모니터/키보드를 연결하거나, 네트워크에서 찾기:

```bash
# 라즈베리 파이에서 실행
hostname -I
# 또는
ip addr show
```

### 3단계: SSH 키 복사

**방법 1: ssh-copy-id 사용 (권장)**

```bash
# 개발 PC에서 실행
# 라즈베리 파이 IP를 알고 있다면
ssh-copy-id -i ~/.ssh/id_rsa.pub juns@192.168.0.XX

# 또는 id_ed25519 키 사용
ssh-copy-id -i ~/.ssh/id_ed25519.pub juns@192.168.0.XX
```

**실행 과정:**
1. 처음 접속 시 "Are you sure you want to continue connecting?" → `yes` 입력
2. 라즈베리 파이 비밀번호 입력 (한 번만)
3. 키가 자동으로 복사됨

**방법 2: 수동 복사**

```bash
# 1. 개발 PC에서 공개 키 내용 복사
cat ~/.ssh/id_rsa.pub
# 또는
cat ~/.ssh/id_ed25519.pub

# 2. 라즈베리 파이에 접속
ssh juns@192.168.0.XX

# 3. 라즈베리 파이에서 실행
mkdir -p ~/.ssh
chmod 700 ~/.ssh
nano ~/.ssh/authorized_keys
# 위에서 복사한 공개 키 내용을 붙여넣기
chmod 600 ~/.ssh/authorized_keys
```

### 4단계: 개발 PC의 /etc/hosts 설정 (편의성)

IP 대신 호스트명으로 접속하기 위해 Mac의 `/etc/hosts` 파일을 수정:

```bash
# Mac에서 실행
sudo nano /etc/hosts
```

다음 내용 추가:

```
# CoinTicker Hadoop Cluster Nodes
192.168.0.100 raspberry-master
192.168.0.101 raspberry-worker1
192.168.0.102 raspberry-worker2
192.168.0.103 raspberry-worker3
```

**이제 호스트명으로 접속 가능**:
```bash
ssh ubuntu@raspberry-master   # 192.168.0.100 대신
ssh ubuntu@raspberry-worker1  # 192.168.0.101 대신
```

### 5단계: 여러 라즈베리 파이에 동일한 키 복사

4대의 라즈베리 파이에 모두 동일한 키를 복사:

```bash
# 호스트명으로 키 복사 (추천)
for host in raspberry-master raspberry-worker1 raspberry-worker2 raspberry-worker3; do
    echo "키 복사 중: $host"
    ssh-copy-id -i ~/.ssh/id_ed25519.pub ubuntu@$host
done
```

또는 IP 주소로 각각 수동으로:

```bash
ssh-copy-id -i ~/.ssh/id_ed25519.pub ubuntu@192.168.0.100  # Master
ssh-copy-id -i ~/.ssh/id_ed25519.pub ubuntu@192.168.0.101  # Worker 1
ssh-copy-id -i ~/.ssh/id_ed25519.pub ubuntu@192.168.0.102  # Worker 2
ssh-copy-id -i ~/.ssh/id_ed25519.pub ubuntu@192.168.0.103  # Worker 3
```

---

## 접속 테스트

### 패스워드 없이 접속 확인

```bash
# 개발 PC에서 실행
ssh juns@192.168.0.XX
# 비밀번호 입력 없이 접속되면 성공!
```

### 모든 노드 접속 테스트

```bash
# 각 라즈베리 파이에 접속 테스트
for ip in 192.168.0.60 192.168.0.61 192.168.0.62 192.168.0.63; do
    echo "테스트: $ip"
    ssh juns@$ip "echo '✅ $ip 접속 성공!' && hostname"
done
```

---

## 요약: 빠른 설정 체크리스트

### 라즈베리 파이 초기 설정

- [ ] Ubuntu Server 설치 시 사용자 이름: `ubuntu` 설정
- [ ] 비밀번호 설정 (안전한 비밀번호)
- [ ] 라즈베리 파이 IP 주소 확인

### 네트워크 설정

- [ ] 고정 IP 설정 (`/etc/netplan/99-static-ip.yaml`)
  - Master: 192.168.0.100
  - Worker1: 192.168.0.101
  - Worker2: 192.168.0.102
  - Worker3: 192.168.0.103
- [ ] 네트워크 인터페이스 확인 (eth0 또는 wlan0)
- [ ] `/etc/hosts` 파일에 클러스터 노드 등록

### 개발 PC에서 SSH 키 설정

- [ ] SSH 키 확인/생성 (`~/.ssh/id_rsa` 또는 `id_ed25519`)
- [ ] 각 라즈베리 파이에 키 복사 (`ssh-copy-id`)
- [ ] 패스워드 없이 접속 테스트
- [ ] `~/.ssh/config` 설정 (선택)

### 외부 접속 (선택사항)

- [ ] 공인 IP 확인 (`curl ifconfig.me`)
- [ ] 공유기 포트포워딩 설정
- [ ] 외부에서 접속 테스트
- [ ] 보안 강화 (Fail2ban, 포트 변경 등)

### 결과

- ✅ 하나의 SSH 키로 모든 라즈베리 파이에 접속 가능
- ✅ 패스워드 입력 없이 접속
- ✅ 고정 IP로 안정적인 네트워크 구성
- ✅ 자동화 스크립트 실행 가능
- ✅ (선택) 외부에서도 접속 가능

---

## 문제 해결

### ssh-copy-id가 작동하지 않을 때

```bash
# 수동으로 복사
cat ~/.ssh/id_rsa.pub | ssh juns@192.168.0.XX "mkdir -p ~/.ssh && cat >> ~/.ssh/authorized_keys"
```

### 권한 문제

라즈베리 파이에서:

```bash
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
```

### 여전히 비밀번호를 요구할 때

라즈베리 파이에서 SSH 설정 확인:

```bash
sudo nano /etc/ssh/sshd_config

# 다음 설정 확인:
# PubkeyAuthentication yes
# AuthorizedKeysFile .ssh/authorized_keys

# 설정 변경 후 재시작
sudo systemctl restart ssh
```

---

## 네트워크 설정 (유선/무선)

### 📡 현재 네트워크 인터페이스 확인

```bash
# 라즈베리파이에서 실행
ip link show

# 출력 예시:
# eth0: <BROADCAST,MULTICAST,UP,LOWER_UP>    # 유선 (활성)
# wlan0: <BROADCAST,MULTICAST>                # 무선 (비활성)
```

### IP 주소 고정 설정

#### 1. 유선 (Ethernet) 고정 IP 설정

```bash
# /etc/netplan/99-static-ip.yaml 생성
sudo nano /etc/netplan/99-static-ip.yaml
```

```yaml
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: no
      addresses:
        - 192.168.0.100/24  # 고정 IP
      routes:
        - to: default
          via: 192.168.0.1   # 공유기 게이트웨이
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
```

```bash
# 권한 설정 (중요!)
sudo chmod 600 /etc/netplan/99-static-ip.yaml

# 적용
sudo netplan apply

# 확인
ip addr show eth0
```

#### 2. 무선 (WiFi) 설정

```yaml
network:
  version: 2
  ethernets:
    eth0:
      dhcp4: no
      addresses:
        - 192.168.0.100/24
      routes:
        - to: default
          via: 192.168.0.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4

  # WiFi 설정 추가
  wifis:
    wlan0:
      dhcp4: no
      addresses:
        - 192.168.0.100/24  # 유선과 동일 IP (동시 사용 시 하나만 활성)
      routes:
        - to: default
          via: 192.168.0.1
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
      access-points:
        "YOUR_WIFI_SSID":  # WiFi 이름
          password: "YOUR_WIFI_PASSWORD"
```

#### 3. 유선/무선 수동 전환

```bash
# 유선 켜기/끄기
sudo ip link set eth0 up    # 켜기
sudo ip link set eth0 down  # 끄기

# 무선 켜기/끄기
sudo ip link set wlan0 up   # 켜기
sudo ip link set wlan0 down # 끄기
```

### 클러스터 IP 할당 계획

| 노드 | 호스트명 | 고정 IP | 인터페이스 |
|------|---------|---------|-----------|
| Master | raspberry-master | 192.168.0.100 | eth0 (유선) |
| Worker 1 | raspberry-worker1 | 192.168.0.101 | eth0 (유선) |
| Worker 2 | raspberry-worker2 | 192.168.0.102 | eth0 (유선) |
| Worker 3 | raspberry-worker3 | 192.168.0.103 | eth0 (유선) |

---

## 외부 접속 설정 (포트포워딩)

### 🌍 외부에서 라즈베리파이 접속하기

로컬 네트워크 외부(카페, 회사, 다른 집)에서 접속하려면 **포트포워딩** 설정이 필요합니다.

### 1. 공인 IP 확인

```bash
# 개발 PC에서 실행
curl ifconfig.me
# 출력 예시: 123.45.67.89
```

### 2. 공유기 포트포워딩 설정

공유기 관리 페이지 접속 (보통 `http://192.168.0.1`):

```
┌─────────────────────────────────────────────┐
│  외부 포트    내부 IP          내부 포트      │
├─────────────────────────────────────────────┤
│  2222    →   192.168.0.100   →   22        │  # Master SSH
│  2223    →   192.168.0.101   →   22        │  # Worker1 SSH
│  2224    →   192.168.0.102   →   22        │  # Worker2 SSH
│  2225    →   192.168.0.103   →   22        │  # Worker3 SSH
│  8080    →   192.168.0.100   →   80        │  # 웹 대시보드 (선택)
│  5000    →   192.168.0.100   →   5000      │  # 백엔드 API (선택)
└─────────────────────────────────────────────┘
```

**설정 예시** (공유기마다 다름):
1. 공유기 관리 페이지 로그인
2. "포트포워딩" 또는 "Port Forwarding" 메뉴
3. 새 규칙 추가:
   - 외부 포트: `2222`
   - 내부 IP: `192.168.0.100`
   - 내부 포트: `22`
   - 프로토콜: `TCP`

### 3. 외부에서 접속

```bash
# 외부 네트워크에서 실행 (카페, 회사 등)
ssh -p 2222 ubuntu@123.45.67.89  # Master
ssh -p 2223 ubuntu@123.45.67.89  # Worker1
ssh -p 2224 ubuntu@123.45.67.89  # Worker2
ssh -p 2225 ubuntu@123.45.67.89  # Worker3
```

### 4. SSH 설정 파일로 편리하게 접속

`~/.ssh/config` 파일 생성:

```bash
# 로컬 접속용
Host rpi-master
    HostName 192.168.0.100
    User ubuntu
    Port 22

Host rpi-worker1
    HostName 192.168.0.101
    User ubuntu
    Port 22

# 외부 접속용
Host rpi-master-external
    HostName 123.45.67.89
    User ubuntu
    Port 2222

Host rpi-worker1-external
    HostName 123.45.67.89
    User ubuntu
    Port 2223
```

**사용**:
```bash
# 로컬에서
ssh rpi-master

# 외부에서
ssh rpi-master-external
```

### ⚠️ 보안 주의사항

외부 접속 시 보안 강화:

1. **SSH 비밀번호 인증 비활성화** (공개키만 허용)
   ```bash
   sudo nano /etc/ssh/sshd_config
   # PasswordAuthentication no
   sudo systemctl restart ssh
   ```

2. **Fail2ban 설치** (무차별 대입 공격 방어)
   ```bash
   sudo apt install fail2ban
   sudo systemctl enable fail2ban
   sudo systemctl start fail2ban
   ```

3. **포트 변경** (기본 22번 포트 대신 다른 포트 사용)
   ```bash
   sudo nano /etc/ssh/sshd_config
   # Port 2200  (예시)
   sudo systemctl restart ssh
   ```

---

## 다음 단계

SSH 키 설정이 완료되면:

1. [RASPBERRY_PI_SD_CLONE_GUIDE.md](./RASPBERRY_PI_SD_CLONE_GUIDE.md) - SD 카드 복제 및 클러스터 설정
2. [DEPLOYMENT_GUIDE.md](./DEPLOYMENT_GUIDE.md) - 전체 배포 가이드

---

**작성자**: PICU 프로젝트 팀
**최종 수정**: 2025-12-02

