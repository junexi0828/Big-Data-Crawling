# CoinTicker React Frontend

CoinTicker 프로젝트의 React 기반 프론트엔드 애플리케이션입니다.

## 🚀 빠른 시작

### 1. 의존성 설치

```bash
cd PICU/cointicker/frontend
npm install
```

### 2. 개발 서버 실행

```bash
npm run dev
```

개발 서버가 `http://localhost:3000`에서 실행됩니다.

### 3. 접속 경로

- `http://localhost:3000/` - 프로젝트 소개 (Home 페이지)
- `http://localhost:3000/app` - 실시간 대시보드
- `http://localhost:3000/app/news` - 뉴스
- `http://localhost:3000/app/insights` - 인사이트
- `http://localhost:3000/app/settings` - 설정

### 4. 정적 HTML 파일 접속

기존 `public/` 폴더의 HTML 파일들도 접근 가능합니다:
- `http://localhost:3000/architecture.html` - 아키텍처 설명
- `http://localhost:3000/performance.html` - 성과 분석
- `http://localhost:3000/demo.html` - 데모

### 3. 프로덕션 빌드

```bash
npm run build
```

빌드된 파일은 `dist/` 디렉토리에 생성됩니다.

## 📁 프로젝트 구조

```
frontend/
├── public/              # 정적 파일 (기존 HTML 유지)
│   ├── index.html      # 프로젝트 소개 (참고용)
│   ├── architecture.html
│   ├── performance.html
│   ├── demo.html
│   └── ...
├── src/
│   ├── components/      # React 컴포넌트
│   │   ├── Layout.jsx   # 레이아웃 컴포넌트
│   │   ├── dashboard/   # 대시보드 컴포넌트
│   │   ├── news/        # 뉴스 컴포넌트
│   │   └── insights/    # 인사이트 컴포넌트
│   ├── pages/           # 페이지 컴포넌트
│   │   ├── Home.jsx     # 프로젝트 소개 (React 버전)
│   │   ├── Dashboard.jsx
│   │   ├── News.jsx
│   │   ├── Insights.jsx
│   │   ├── Settings.jsx
│   │   └── NotFound.jsx
│   ├── services/         # API 서비스
│   │   └── api.js       # API 클라이언트
│   ├── App.jsx          # 메인 앱 컴포넌트
│   ├── main.jsx        # 진입점
│   └── index.css       # 전역 스타일
├── package.json
├── vite.config.js      # Vite 설정
└── README.md
```

## 🎨 주요 기능

### 1. 대시보드
- 실시간 요약 정보
- 감성 분석 추이 차트
- 시장 개요

### 2. 뉴스
- 최신 뉴스 목록
- 감성 분석 표시
- 키워드 태그

### 3. 인사이트
- 투자 인사이트 목록
- 인사이트 생성 기능
- 신뢰도 표시

### 4. 설정
- API 서버 주소 설정
- 새로고침 간격 설정

## 🔧 설정

### 환경 변수

`.env` 파일을 생성하여 설정할 수 있습니다:

```env
VITE_API_BASE_URL=http://localhost:5000
```

### API 프록시

개발 서버는 `vite.config.js`에서 설정한 프록시를 통해 API 요청을 전달합니다.

## 📚 사용 기술

- **React 18**: UI 라이브러리
- **React Router**: 라우팅
- **Vite**: 빌드 도구
- **Chart.js**: 차트 라이브러리
- **Axios**: HTTP 클라이언트
- **date-fns**: 날짜 처리

## 🐛 문제 해결

### 백엔드 연결 실패

1. 백엔드 서버가 실행 중인지 확인
2. `VITE_API_BASE_URL` 환경 변수 확인
3. CORS 설정 확인

### 빌드 오류

```bash
# node_modules 삭제 후 재설치
rm -rf node_modules
npm install
```

## 📝 개발 가이드

### 새 컴포넌트 추가

1. `src/components/` 디렉토리에 컴포넌트 생성
2. 필요한 CSS 파일 생성
3. 컴포넌트 import 및 사용

### API 엔드포인트 추가

`src/services/api.js`에 새로운 API 함수 추가:

```javascript
export const newAPI = {
  getData: async () => {
    return api.get('/api/new-endpoint')
  },
}
```

## 📄 라이선스

MIT License

